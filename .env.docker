# ============================================================================
# UCM Docker Environment Variables
# ============================================================================
# This file contains environment variable overrides for Docker Compose
# Copy this file to .env and customize for your deployment
# ============================================================================

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Your domain name (CRITICAL for Docker deployments)
UCM_FQDN=ucm.example.com

# Network ports
UCM_HTTPS_PORT=8443
UCM_HTTP_PORT=8080

# ============================================================================
# SECURITY SETTINGS (GENERATE UNIQUE VALUES!)
# ============================================================================

# Generate with: openssl rand -hex 32
# UCM_SECRET_KEY=
# UCM_JWT_SECRET=

# Or leave empty for auto-generation on first run

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# Debug mode (set to false in production)
UCM_DEBUG=false
UCM_LOG_LEVEL=INFO

# SMTP Configuration (for email notifications)
UCM_SMTP_ENABLED=false
# UCM_SMTP_SERVER=smtp.gmail.com
# UCM_SMTP_PORT=587
# UCM_SMTP_USERNAME=your-email@gmail.com
# UCM_SMTP_PASSWORD=your-app-password
# UCM_SMTP_FROM=noreply@ucm.example.com
# UCM_SMTP_TLS=true

# Performance
UCM_CACHE_ENABLED=true

# Backups
UCM_BACKUP_ENABLED=true
UCM_BACKUP_RETENTION_DAYS=30

# Protocols
UCM_ACME_ENABLED=true

# ============================================================================
# ADVANCED SETTINGS
# ============================================================================

# mTLS (configure via web UI first)
# UCM_MTLS_ENABLED=false

# Custom HTTPS certificates (paste PEM content or mount as volumes)
# UCM_HTTPS_CERT=
# UCM_HTTPS_KEY=

# ============================================================================
# NOTES
# ============================================================================
#
# Quick Start:
#   1. Set UCM_FQDN to your domain
#   2. Run: docker-compose up -d
#   3. Access: https://your-domain:8443
#   4. Login: admin / changeme123
#   5. CHANGE PASSWORD IMMEDIATELY!
#
# For production:
#   docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# For development:
#   docker-compose -f docker-compose.yml -f docker-compose.dev.yml up
#
# ============================================================================
