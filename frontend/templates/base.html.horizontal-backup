<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Ultimate CA Manager{% endblock %}</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="{{ url_for('static', filename='favicon.svg') }}">
    
    <!-- Embedded Resources - NO CDN - NO TAILWIND -->
    <link rel="stylesheet" href="{{ url_for('static', filename='vendor/css/fontawesome.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/components.css') }}">
    <link id="theme-css" rel="stylesheet" href="{{ url_for('static', filename='css/themes/sentinel-light.css') }}">
    
    {% block extra_head %}{% endblock %}
</head>
<body>
    
    {% if request.endpoint and request.endpoint != 'ui.login' %}
    
    <!-- Navbar -->
    <nav class="navbar">
        <div class="navbar-container">
            <!-- Logo -->
            <div class="navbar-brand">
                <div class="ucm-logo">
                    <i class="fas fa-shield-halved"></i>
                </div>
                <span class="brand-text">Ultimate CA Manager</span>
            </div>
            
            <!-- Main Navigation -->
            <div class="navbar-menu">
                <a href="/dashboard" class="nav-link {% if request.path == '/dashboard' %}active{% endif %}">
                    <i class="fas fa-gauge-high"></i>
                    <span>Dashboard</span>
                </a>
                <a href="/ca" class="nav-link {% if request.path.startswith('/ca') %}active{% endif %}">
                    <i class="fas fa-lock"></i>
                    <span>CAs</span>
                </a>
                <a href="/certificates" class="nav-link {% if request.path.startswith('/certificates') or request.path.startswith('/certs') %}active{% endif %}">
                    <i class="fas fa-certificate"></i>
                    <span>Certificates</span>
                </a>
                <a href="/scep" class="nav-link {% if request.path.startswith('/scep') %}active{% endif %}">
                    <i class="fas fa-server"></i>
                    <span>SCEP</span>
                </a>
                <a href="/import" class="nav-link {% if request.path.startswith('/import') %}active{% endif %}">
                    <i class="fas fa-download"></i>
                    <span>Import</span>
                </a>
                <a href="/config" class="nav-link {% if request.path.startswith('/config') %}active{% endif %}">
                    <i class="fas fa-sliders"></i>
                    <span>Config</span>
                </a>
            </div>
            
            <!-- Right Side -->
            <div class="navbar-actions">
                <!-- Theme Switcher -->
                <div class="dropdown" x-data="{ open: false }">
                    <button @click="open = !open" class="btn-icon" title="Change Theme">
                        <i class="fas fa-palette"></i>
                    </button>
                    <div x-show="open" @click.away="open = false" x-cloak class="dropdown-menu">
                        <div class="dropdown-header">Themes</div>
                        <a href="#" class="dropdown-item" onclick="switchTheme('sentinel-light'); return false;">
                            <i class="fas fa-sun"></i> Sentinel Light
                        </a>
                        <a href="#" class="dropdown-item" onclick="switchTheme('sentinel-dark'); return false;">
                            <i class="fas fa-moon"></i> Sentinel Dark
                        </a>
                        <a href="#" class="dropdown-item" onclick="switchTheme('amber-light'); return false;">
                            <i class="fas fa-sun"></i> Amber Light
                        </a>
                        <a href="#" class="dropdown-item" onclick="switchTheme('amber-dark'); return false;">
                            <i class="fas fa-moon"></i> Amber Dark
                        </a>
                        <a href="#" class="dropdown-item" onclick="switchTheme('nebula-light'); return false;">
                            <i class="fas fa-sun"></i> Nebula Light
                        </a>
                        <a href="#" class="dropdown-item" onclick="switchTheme('nebula-dark'); return false;">
                            <i class="fas fa-moon"></i> Nebula Dark
                        </a>
                        <a href="#" class="dropdown-item" onclick="switchTheme('blossom-light'); return false;">
                            <i class="fas fa-sun"></i> Blossom Light
                        </a>
                        <a href="#" class="dropdown-item" onclick="switchTheme('blossom-dark'); return false;">
                            <i class="fas fa-moon"></i> Blossom Dark
                        </a>
                    </div>
                </div>
                
                <!-- User Menu -->
                <div class="dropdown" x-data="{ open: false }">
                    <button @click="open = !open" class="user-menu-button">
                        <div class="user-avatar">
                            {{ session.get('username', 'U')[0].upper() }}
                        </div>
                        <span class="user-name">{{ session.get('username', 'User') }}</span>
                        <i class="fas fa-chevron-down"></i>
                    </button>
                    <div x-show="open" @click.away="open = false" x-cloak class="dropdown-menu">
                        <div class="dropdown-header">
                            <div>{{ session.get('username', 'User') }}</div>
                            <div class="text-xs text-muted">{{ session.get('role', 'User').capitalize() }}</div>
                        </div>
                        <a href="/settings" class="dropdown-item">
                            <i class="fas fa-gear"></i> Settings
                        </a>
                        <a href="/users" class="dropdown-item">
                            <i class="fas fa-user-group"></i> Users
                        </a>
                        <hr class="dropdown-divider">
                        <form action="/logout" method="POST">
                            <button type="submit" class="dropdown-item text-danger">
                                <i class="fas fa-right-from-bracket"></i> Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </nav>
    
    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
        <div class="flash-messages">
            {% for category, message in messages %}
            <div x-data="{ show: true }" x-show="show" x-transition class="alert alert-{{ category }}">
                <div class="d-flex items-center justify-between">
                    <span>{{ message }}</span>
                    <button @click="show = false" class="btn-icon btn-sm">
                        <i class="fas fa-xmark"></i>
                    </button>
                </div>
            </div>
            {% endfor %}
        </div>
        {% endif %}
    {% endwith %}
    
    <!-- Main Content -->
    <main class="main-content">
        {% block content %}{% endblock %}
    </main>
    
    {% else %}
    
    <!-- Login Page -->
    {% block login_content %}{% endblock %}
    
    {% endif %}
    
    <!-- Scripts -->
    <script src="{{ url_for('static', filename='vendor/js/htmx.min.js') }}"></script>
    <script src="{{ url_for('static', filename='vendor/js/alpine.min.js') }}" defer></script>
    <script src="{{ url_for('static', filename='js/icon-system.js') }}"></script>
    <script src="{{ url_for('static', filename='js/theme-switcher.js') }}"></script>
    
    <style>
        [x-cloak] { display: none !important; }
    </style>
    
    {% block extra_body %}{% endblock %}
</body>
</html>
